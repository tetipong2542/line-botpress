# 📋 Plan-1401: ฟีเจอร์สินเชื่อ (Loan Feature) - วิเคราะห์และเสนอแนะ

## 📅 วันที่วิเคราะห์
2026-01-14

---

## 🎯 สรุปการวิเคราะห์โปรเจค

### สถานะปัจจุบัน
แอปพลิเคชันเป็นระบบบันทึกรายรับ-รายจ่ายผ่าน LINE Chatbot และ Web App ที่มีความสามารถดังนี้:

| หน้า | ฟังก์ชัน |
|------|----------|
| `/app` (Dashboard) | ภาพรวมรายรับ-รายจ่าย, AI Insights, Financial Health Score, Quick Entry |
| `/recurring` | จัดการรายการประจำ (Netflix, ค่าเช่า, เงินเดือน) |
| `/analytics` | รายงานและกราฟวิเคราะห์ |
| `/budgets` | จัดการงบประมาณรายหมวด |
| `/goals` | เป้าหมายออมเงิน (Savings Goals) |
| `/categories` | จัดการหมวดหมู่รายรับ-รายจ่าย |
| `/profile` | ตั้งค่าโปรไฟล์ผู้ใช้ |

### ข้อจำกัดปัจจุบัน
- **Footer Menu เต็ม 5 เมนู**: หน้าแรก, ประจำ, รายงาน, หมวดหมู่, โปรไฟล์
- ไม่มีฟีเจอร์คำนวณสินเชื่อ/ดอกเบี้ย
- หน้า app.html เป็นเพียง welcome page ไม่ได้ใช้งานจริง

---

## 💡 เสนอ 3 Options สำหรับฟีเจอร์สินเชื่อ

---

### 🏆 Option 1: เพิ่มใน `/recurring` เป็น Tab ย่อย (⭐ แนะนำ)

> [!TIP]
> **ดีที่สุดสำหรับ UX** - ไม่ต้องเพิ่มเมนูใหม่ ใช้โครงสร้างที่มีอยู่

#### แนวคิด
เพิ่ม Tab Switcher ด้านบนหน้า `/recurring`:
- **Tab 1: รายการประจำ** (ฟังก์ชันเดิม)
- **Tab 2: สินเชื่อ/ผ่อนชำระ** (ฟังก์ชันใหม่)

#### ฟีเจอร์สินเชื่อ
1. **บันทึกสินเชื่อ**
   - ชื่อสินเชื่อ (เช่น บ้าน, รถ, บัตรเครดิต)
   - ยอดเงินต้น (Principal)
   - อัตราดอกเบี้ยต่อปี (% หรือ flat rate)
   - ระยะเวลาผ่อน (เดือน)
   - วันที่เริ่มต้น

2. **คำนวณอัตโนมัติ**
   - ค่างวดรายเดือน
   - ดอกเบี้ยรวม
   - ยอดรวมที่ต้องชำระ
   - ตารางผ่อนชำระ (Amortization Table)

3. **แสดงผลใน Dashboard**
   - สรุปสินเชื่อทั้งหมด
   - งวดถัดไปที่ต้องจ่าย
   - Progress ที่ผ่อนไปแล้ว

#### ข้อดี
- ✅ ไม่เพิ่มเมนูใน Footer
- ✅ เข้ากันได้กับ "รายการประจำ" (เป็นรายจ่ายที่เกิดซ้ำ)
- ✅ UX เข้าใจง่าย

#### ข้อเสีย
- ⚠️ หน้า recurring อาจรกขึ้น

#### Mockup UI
```
┌─────────────────────────────────────┐
│ 🔄 รายการประจำ                      │
├─────────────────────────────────────┤
│ [รายการประจำ] [สินเชื่อ/ผ่อนชำระ]   │  ← Tab Switcher
├─────────────────────────────────────┤
│ ┌─────────────────────────────────┐ │
│ │ 🏠 สินเชื่อบ้าน                 │ │
│ │ ยอดคงเหลือ: ฿1,500,000         │ │
│ │ งวดถัดไป: ฿15,000 (15 ม.ค.)    │ │
│ │ ดอกเบี้ย: 3.5%/ปี              │ │
│ │ ▓▓▓▓▓░░░░░ 45% (54/120 งวด)   │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

---

### ⚙️ Option 2: เปลี่ยน `/profile` → ย้ายไปเมนูอื่น + เพิ่มเมนู "เครื่องมือ"

> [!IMPORTANT]
> **ปรับ UX ใหม่** - รวมเครื่องมือการเงินไว้ด้วยกัน

#### แนวคิด
ปรับ Footer Menu:
```
[หน้าแรก] [ประจำ] [รายงาน] [เครื่องมือ] [เพิ่มเติม ⋮]
```

- **เครื่องมือ**: สินเชื่อ, คำนวณดอกเบี้ย, วางแผนการเงิน
- **เพิ่มเติม (⋮)**: โปรไฟล์, หมวดหมู่, งบประมาณ, เป้าหมาย, ช่วยเหลือ

#### ฟีเจอร์สินเชื่อ (หน้าแยก `/tools/loans`)
1. **รายการสินเชื่อ**
2. **เครื่องคำนวณดอกเบี้ย** (ไม่ต้องบันทึก)
   - คำนวณดอกเบี้ยแบบลดต้นลดดอก
   - คำนวณดอกเบี้ยแบบคงที่ (Flat Rate)
   - เปรียบเทียบ 2 สินเชื่อ
3. **AI วิเคราะห์สินเชื่อ**
   - แนะนำว่าควรรีไฟแนนซ์ไหม
   - คำนวณ Break-even point

#### ข้อดี
- ✅ จัดระเบียบเมนูใหม่ให้เป็นระบบ
- ✅ มี "เครื่องคำนวณ" ที่ใช้ได้เลยไม่ต้องบันทึก
- ✅ ขยายได้ง่ายในอนาคต

#### ข้อเสีย
- ⚠️ ต้องปรับ Footer ทั้งหมด
- ⚠️ ผู้ใช้เดิมต้องปรับตัว

#### Mockup UI
```
┌─────────────────────────────────────┐
│ 🛠️ เครื่องมือการเงิน                │
├─────────────────────────────────────┤
│ ┌───────────────┬─────────────────┐ │
│ │ 💳 สินเชื่อ   │ 🧮 คำนวณดอกเบี้ย│ │
│ ├───────────────┴─────────────────┤ │
│ │ 📊 เปรียบเทียบ│ 📈 วางแผน      │ │
│ └─────────────────────────────────┘ │
│                                     │
│ [คำนวณดอกเบี้ยแบบ Quick]            │
│ ยอดเงินกู้: [________] บาท          │
│ ดอกเบี้ย:  [___] % ต่อปี            │
│ ระยะเวลา: [___] เดือน              │
│                                     │
│ [คำนวณ →]                           │
│                                     │
│ 💡 ค่างวด: ฿15,000/เดือน            │
│    ดอกเบี้ยรวม: ฿180,000            │
└─────────────────────────────────────┘
```

---

### 📊 Option 3: เพิ่มใน Dashboard (`/app`) เป็น Section ใหม่

> [!NOTE]
> **วิธีง่ายที่สุด** - เพิ่ม section โดยไม่แตะโครงสร้างเดิม

#### แนวคิด
เพิ่ม Section "สินเชื่อ/ผ่อนชำระ" ใน Dashboard หลังจาก Quick Actions

#### ฟีเจอร์
1. **Quick Loan Calculator** (คำนวณเร็ว)
   - ใส่ยอดเงิน, ดอกเบี้ย, เดือน → เห็นค่างวดทันที

2. **สรุปสินเชื่อที่บันทึก** (ถ้ามี)
   - ยอดคงเหลือทั้งหมด
   - งวดถัดไปที่ต้องจ่าย

3. **ลิงก์ไปหน้าจัดการสินเชื่อ** (`/loans`)
   - หน้าแยกสำหรับจัดการสินเชื่อทั้งหมด
   - เข้าถึงจาก Dashboard หรือจากเมนู "เพิ่มเติม" ใน Profile

#### ข้อดี
- ✅ ไม่แก้ Footer เลย
- ✅ เห็นข้อมูลสินเชื่อบน Dashboard ทันที
- ✅ คำนวณเร็วได้โดยไม่ต้องบันทึก

#### ข้อเสีย
- ⚠️ Dashboard อาจยาวเกินไป
- ⚠️ หน้าจัดการสินเชื่อซ่อนอยู่ (ไม่มีใน Footer)

#### Mockup UI (ใน Dashboard)
```
┌─────────────────────────────────────┐
│ 💰 สินเชื่อและผ่อนชำระ             │
├─────────────────────────────────────┤
│ ┌─────────────────┬───────────────┐ │
│ │ ยอดหนี้คงเหลือ  │ งวดถัดไป      │ │
│ │ ฿2,350,000     │ ฿28,500       │ │
│ │                │ 15 ม.ค. 69    │ │
│ └─────────────────┴───────────────┘ │
│                                     │
│ 🧮 คำนวณเร็ว                         │
│ ยอดกู้: [1,000,000]  ดอกเบี้ย: [5%] │
│ เดือน: [36]  → ค่างวด: ฿29,970     │
│                                     │
│ [ดูสินเชื่อทั้งหมด →]                │
└─────────────────────────────────────┘
```

---

## 📊 เปรียบเทียบ 3 Options

| เกณฑ์ | Option 1 | Option 2 | Option 3 |
|-------|----------|----------|----------|
| **ความง่ายในการพัฒนา** | ⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐⭐⭐ |
| **UX/ความเข้าใจง่าย** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| **ไม่กระทบโครงสร้างเดิม** | ⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐⭐⭐ |
| **ขยายได้ในอนาคต** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| **การเข้าถึงฟีเจอร์** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |

---

## ✅ คำแนะนำ

> [!IMPORTANT]
> **แนะนำ Option 1** - ดีที่สุดสำหรับ UX และความเข้ากันได้กับโครงสร้างเดิม

### เหตุผล
1. **สินเชื่อ = รายจ่ายประจำ** - เข้ากันได้กับแนวคิดหน้า recurring
2. **ไม่ต้องแก้ Footer** - ลดความเสี่ยงและความซับซ้อน
3. **ผู้ใช้หาง่าย** - คนที่ดูรายจ่ายประจำจะเห็นสินเชื่อด้วย
4. **พัฒนาเร็ว** - ใช้โครงสร้าง recurring.html ที่มีอยู่

### ฟีเจอร์ที่ควรมี (MVP)
1. ✅ บันทึกสินเชื่อ (ชื่อ, ยอดเงินต้น, ดอกเบี้ย, ระยะเวลา)
2. ✅ คำนวณค่างวดอัตโนมัติ
3. ✅ แสดงตารางผ่อนชำระ
4. ✅ บันทึกการชำระ (similar to goal contribution)
5. ✅ แสดงสรุปใน Dashboard

---

## 🔧 Technical Specification (Option 1)

### Database: New Table `loans`
```python
class Loan(db.Model):
    id = db.Column(db.String(36), primary_key=True)
    project_id = db.Column(db.String(36), ForeignKey)
    name = db.Column(db.String(100))  # ชื่อสินเชื่อ
    principal = db.Column(db.Integer)  # ยอดเงินต้น (satang)
    interest_rate = db.Column(db.Float)  # ดอกเบี้ยต่อปี (%)
    interest_type = db.Column(db.String(20))  # 'reducing' | 'flat'
    term_months = db.Column(db.Integer)  # ระยะเวลาผ่อน
    start_date = db.Column(db.Date)
    monthly_payment = db.Column(db.Integer)  # ค่างวด (satang)
    paid_installments = db.Column(db.Integer, default=0)
    is_active = db.Column(db.Boolean, default=True)
    created_at = db.Column(db.DateTime)
```

### API Endpoints
- `GET /api/v1/projects/<id>/loans` - List loans
- `POST /api/v1/projects/<id>/loans` - Create loan
- `PUT /api/v1/projects/<id>/loans/<loan_id>` - Update loan
- `DELETE /api/v1/projects/<id>/loans/<loan_id>` - Delete loan
- `POST /api/v1/projects/<id>/loans/<loan_id>/pay` - Record payment
- `GET /api/v1/projects/<id>/loans/<loan_id>/schedule` - Get amortization schedule
- `POST /api/v1/projects/<id>/loans/calculate` - Calculate loan (without saving)

### Frontend Changes
1. แก้ไข `recurring.html` เพิ่ม Tab Switcher
2. สร้าง JavaScript สำหรับ Loan Management
3. เพิ่ม Section สรุปสินเชื่อใน `dashboard.html`

---

## 📝 Next Steps

หากต้องการดำเนินการต่อ กรุณาเลือก Option และยืนยัน จากนั้นจะสร้าง Implementation Plan แบบละเอียด
