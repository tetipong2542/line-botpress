# 🚀 Quick Start Guide

## การเริ่มต้นใช้งาน

### 1. Initialize Database (ถ้ายังไม่เคยทำ)

```bash
# Option A: Using Flask CLI
export FLASK_APP=run.py
flask init-db

# Option B: Using Python script
python3 -c "from app import create_app, db; app = create_app(); app.app_context().push(); db.create_all(); print('✅ Database initialized!')"
```

### 2. Start Server

```bash
# หยุดเซิร์ฟเวอร์เดิม (ถ้ามี)
pkill -f "python.*run.py"

# เริ่มเซิร์ฟเวอร์ใหม่
python run.py
```

เซิร์ฟเวอร์จะทำงานที่: **http://127.0.0.1:5000**

### 3. Login via LINE

1. เปิดเบราว์เซอร์ไปที่: http://127.0.0.1:5000/auth/line
2. Login ด้วย LINE account
3. **ระบบจะสร้างโปรเจคพร้อมหมวดหมู่เริ่มต้นให้อัตโนมัติ!** ✨

**โปรเจคเริ่มต้นจะมี:**
- ชื่อ: "บัญชี [ชื่อของคุณ]"
- หมวดหมู่รายจ่าย 6 รายการ: อาหาร, เดินทาง, ช้อปปิ้ง, บันเทิง, สาธารณูปโภค, สุขภาพ
- หมวดหมู่รายรับ 3 รายการ: เงินเดือน, รายได้เสริม, ของขวัญ

### 4. เริ่มใช้งาน!

หลัง Login สำเร็จ จะถูก redirect ไปที่ `/app` (Dashboard)

**ทดลองใช้งาน:**

#### 📝 เพิ่มรายจ่าย
1. กดปุ่ม **"เพิ่มรายจ่าย"** สีแดง
2. เลือกหมวดหมู่ (จะโหลดจาก database!)
3. กรอกจำนวนเงิน
4. กดบันทึก
5. ✅ จะเห็น Toast "บันทึกรายจ่ายสำเร็จ!" สีเขียว

#### 📝 เพิ่มรายการประจำ
1. ไปที่เมนู **"รายการประจำ"** (ไอคอน 🔄)
2. กดปุ่ม **+** ล่างขวา
3. เลือกประเภท: รายจ่าย
4. เลือกหมวดหมู่: เช่น "💡 ค่าสาธารณูปโภค"
5. เลือกความถี่: ทุกเดือน
6. วันที่: 1
7. จำนวนเงิน: 299
8. กดบันทึก
9. ✅ จะเห็น Toast "บันทึกรายการประจำสำเร็จ!"

#### 📂 จัดการหมวดหมู่
1. ไปที่เมนู **"หมวดบัญชี"** (ไอคอน 📁)
2. กดปุ่ม **+** ล่างขวา
3. กรอกชื่อ: เช่น "ค่าที่พัก"
4. กดบันทึก
5. ✅ จะเห็นหมวดหมู่ใหม่ในรายการ!

#### 👥 เชิญสมาชิก
1. ไปที่เมนู **"สมาชิก"** (ไอคอน 👥)
2. กดปุ่ม **+** ล่างขวา
3. กรอกอีเมล: friend@example.com
4. เลือกบทบาท: สมาชิก
5. กดส่งคำเชิญ
6. ✅ จะเห็น Toast "ส่งคำเชิญสำเร็จ!"

---

## 🔍 ตรวจสอบว่าทำงานถูกต้อง

### เปิด Browser DevTools (F12)

1. เปิด Console tab
2. ควรเห็นข้อความเหล่านี้:
   ```
   Categories loaded: [...]
   Transactions loaded: [...]
   Recurring rules loaded: [...]
   ```

3. **ไม่ควรมี errors สีแดง**

### ตรวจสอบ Network Tab

1. เปิด Network tab (F12)
2. กดปุ่ม "เพิ่มรายจ่าย"
3. เลือกหมวดหมู่
4. ควรเห็น request:
   - `GET /api/v1/projects/[id]/categories` → Status 200 ✅

5. กรอกข้อมูลและบันทึก
6. ควรเห็น request:
   - `POST /api/v1/projects/[id]/transactions` → Status 201 ✅

---

## ⚠️ Troubleshooting

### ปัญหา: เห็น "Project ID is required" ใน Console

**สาเหตุ:** ผู้ใช้ไม่มีโปรเจค

**แก้ไข:**
1. Logout: `POST http://127.0.0.1:5000/auth/logout`
2. Login ใหม่: `GET http://127.0.0.1:5000/auth/line`
3. ระบบจะสร้างโปรเจคให้อัตโนมัติ

### ปัญหา: Toast ไม่แสดง

**สาเหตุ:** CSS ยังไม่โหลด หรือ JavaScript error

**แก้ไข:**
1. Hard refresh: `Cmd+Shift+R` (Mac) หรือ `Ctrl+Shift+R` (Windows)
2. Clear cache
3. เช็ค Console หา errors

### ปัญหา: หมวดหมู่ว่างเปล่า

**สาเหตุ:** Categories ยังไม่ถูกสร้าง

**แก้ไข:**
```bash
python3 << 'EOF'
from app import create_app, db
from app.models.project import Project
from app.models.category import Category
from app.models.user import User

app = create_app()
with app.app_context():
    user = User.query.first()
    project = Project.query.filter_by(owner_user_id=user.id).first()

    if project:
        default_categories = [
            {'type': 'expense', 'name_th': 'อาหารและเครื่องดื่ม', 'icon': '🍔', 'color': '#EF4444'},
            {'type': 'expense', 'name_th': 'ค่าเดินทาง', 'icon': '🚗', 'color': '#F59E0B'},
            {'type': 'expense', 'name_th': 'ช้อปปิ้ง', 'icon': '🛍️', 'color': '#EC4899'},
            {'type': 'expense', 'name_th': 'ความบันเทิง', 'icon': '🎬', 'color': '#8B5CF6'},
            {'type': 'expense', 'name_th': 'ค่าสาธารณูปโภค', 'icon': '💡', 'color': '#10B981'},
            {'type': 'expense', 'name_th': 'สุขภาพ', 'icon': '🏥', 'color': '#06B6D4'},
            {'type': 'income', 'name_th': 'เงินเดือน', 'icon': '💰', 'color': '#10B981'},
            {'type': 'income', 'name_th': 'รายได้เสริม', 'icon': '📈', 'color': '#3B82F6'},
            {'type': 'income', 'name_th': 'ของขวัญ', 'icon': '🎁', 'color': '#EC4899'},
        ]

        for idx, cat_data in enumerate(default_categories):
            category = Category(
                project_id=project.id,
                type=cat_data['type'],
                name_th=cat_data['name_th'],
                icon=cat_data['icon'],
                color=cat_data['color'],
                sort_order=idx + 1
            )
            db.session.add(category)

        db.session.commit()
        print(f"✅ Created {len(default_categories)} categories for project: {project.name}")
EOF
```

### ปัญหา: API ส่ง 500 Internal Server Error

**แก้ไข:**
1. เช็ค server logs
2. เช็ค browser Console → Network tab → Response
3. ถ้าเป็น Import error → รีสตาร์ทเซิร์ฟเวอร์

```bash
pkill -f "python.*run.py"
python run.py
```

---

## 📚 อ่านเพิ่มเติม

- **IMPLEMENTATION_COMPLETE.md** - รายละเอียดฟีเจอร์ที่พัฒนาครบ
- **README.md** - คู่มือการติดตั้งและ deployment
- **SETUP.md** - ขั้นตอนการตั้งค่าระบบ

---

## ✅ Checklist

หลังจากทำตาม Quick Start แล้ว คุณควรจะ:

- [ ] Login ด้วย LINE สำเร็จ
- [ ] เห็นหน้า Dashboard
- [ ] มีโปรเจคชื่อ "บัญชี [ชื่อคุณ]"
- [ ] มีหมวดหมู่ 9 รายการ
- [ ] เพิ่มรายจ่ายได้ + เห็น Toast สีเขียว
- [ ] เพิ่มรายรับได้ + เห็น Toast สีเขียว
- [ ] เพิ่มรายการประจำได้
- [ ] เพิ่มหมวดหมู่ใหม่ได้
- [ ] ส่งคำเชิญสมาชิกได้
- [ ] ไม่มี JavaScript errors ใน Console

**ถ้าทั้งหมดผ่าน = ระบบพร้อมใช้งานแล้ว! 🎉**

---

**Last Updated:** 2026-01-03
**Status:** ✅ Ready to Use
